# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Python application

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
    

  
  
  
permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python 3.10
      uses: actions/setup-python@v3
      with:
        python-version: "3.10"
    - name: Install dependencies
      run: 
        pip install -r requirements.txt
    - name: get id
      run: git log -n1 --format="%H" > output.log
    - name: Upload output file
      uses: actions/upload-artifact@v2 #task predefinito
      with:
        name: output-log-file
        path: output.log
    - name: commit classification
      run: python3 newCommitClassification.py 99f0f9be5cca2fd58f3eaf3921a412de0bc64128 https://github.com/R-luigi99/Prova.git
      #per mettere output
   
